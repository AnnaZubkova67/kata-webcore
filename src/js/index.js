import '../scss/style.scss';
import {buttonCall , modalCall, modalFeedback, modalMenu, buttonChat, buttonMenu , bodyContent, openModal} from "./open_modal";
import {linkOnwards,content, openContent} from "./open_content";
import {modal, modalContent,closed, closedModal} from "./closed_modal";
import {slider, sliderBrands, sliderTypes, sliderServices, newSwiper, swiper} from "./swiper";

